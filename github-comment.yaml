# https://github.com/suzuki-shunsuke/github-comment
post:
  generate-registry:
    template: |
      # :x: Run generate-registry.sh

      {{template "link" .}}

      [registry.yaml on the repository root directory](https://github.com/aquaproj/aqua-registry/blob/main/registry.yaml) is generated with [generate-registry.sh](https://github.com/aquaproj/aqua-registry/blob/main/generate-registry.sh).
      Don't edit it manually, and if you update `registry.yaml` in [pkgs](https://github.com/aquaproj/aqua-registry/tree/main/pkgs) directory, don't forget to run generate-registry.sh.

      ```console
      $ bash generate-registry.sh
      ```
