---
# the configuration file of cmdx - task runner
# https://github.com/suzuki-shunsuke/cmdx
tasks:
  - name: install
    short: i
    description: install all packages
    usage: install all packages
    script: clienv -c clienv-all.yaml i --test
  - name: test
    short: t
    description: test
    usage: test
    script: clienv i --test
  - name: exec
    short: x
    description: exec with clienv-all.yaml
    usage: exec with clienv-all.yaml
    script: AQUA_CONFIG=clienv-all.yaml clienv exec -- {{._builtin.args_string}}
  - name: which
    short: w
    description: clienv which with clienv-all.yaml
    usage: clienv which with clienv-all.yaml
    script: "AQUA_CONFIG=clienv-all.yaml clienv which {{.package}}"
    args:
      - name: package
        required: true
  - name: generate
    short: g
    description: clienv generate with clienv-all.yaml
    usage: clienv generate with clienv-all.yaml
    script: clienv -c clienv-all.yaml g
  - name: release
    short: r
    description: release the new version
    usage: release the new version
    script: |
      git tag "{{.version}}"
      git push origin "{{.version}}"
    args:
      - name: version
        required: true
        validate:
          - regexp: "^v\\d+\\.\\d+.\\d+(-\\d+)?$"
  - name: registry
    description: Merge registry.yaml
    usage: Merge registry.yaml
    script: bash generate-registry.sh
