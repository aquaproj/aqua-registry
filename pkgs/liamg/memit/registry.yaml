packages:
  - type: github_release
    repo_owner: liamg
    repo_name: memit
    asset: "memit-{{.OS}}-{{.Arch}}"
    format: raw
    description: Run binaries straight from memory in Linux
    supported_if: GOOS == "linux"
