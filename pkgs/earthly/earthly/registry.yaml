packages:
  - type: github_release
    repo_owner: earthly
    repo_name: earthly
    description: Repeatable builds
    supported_if: not (GOOS == "windows" and GOARCH == "arm64")
    asset: "earthly-{{.OS}}-{{.Arch}}"
