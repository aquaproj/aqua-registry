packages:
  - type: github_release
    repo_owner: tkuchiki
    repo_name: alp
    rosetta2: true
    asset: "alp_{{.OS}}_{{.Arch}}.zip"
    supported_if: not (GOOS == "linux" and GOARCH == "arm64")
    description: Access Log Profiler
