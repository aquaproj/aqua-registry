packages:
  - type: github_release
    repo_owner: sigstore
    repo_name: cosign
    format: raw
    asset: cosign-{{.OS}}-{{.Arch}}
    description: Container Signing
    supported_envs:
      - darwin
      - linux
      - amd64
