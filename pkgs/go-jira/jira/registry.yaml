packages:
  - type: github_release
    repo_owner: go-jira
    repo_name: jira
    rosetta2: true
    supported_if: not (GOOS == "linux" and GOARCH == "arm64")
    asset: "jira-{{.OS}}-{{.Arch}}"
    description: simple jira command line client in Go
