packages:
  - type: github_release
    repo_owner: containers
    repo_name: gvisor-tap-vsock
    description: A new network stack based on gVisor
    asset: gvproxy-{{.OS}}
    format: raw
    supported_envs:
      - darwin
      - amd64
    rosetta2: true
