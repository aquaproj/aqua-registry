packages:
  - type: github_release
    repo_owner: slok
    repo_name: sloth
    asset: sloth-{{.OS}}-{{.Arch}}
    description: Easy and simple Prometheus SLO (service level objectives) generator
    format: raw
