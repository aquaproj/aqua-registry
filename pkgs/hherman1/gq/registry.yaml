packages:
  - type: github_release
    repo_owner: hherman1
    repo_name: gq
    asset: "{{.OS}}-{{.Arch}}.zip"
    description: jq but using go instead
    supported_envs:
      - darwin
      - amd64
