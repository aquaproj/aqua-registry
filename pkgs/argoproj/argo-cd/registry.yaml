packages:
  - type: github_release
    repo_owner: argoproj
    repo_name: argo-cd
    asset: "argocd-{{.OS}}-amd64"
    description: Declarative continuous deployment for Kubernetes
    files:
      - name: argocd
        src: "argocd-{{.OS}}-{{.Arch}}"
