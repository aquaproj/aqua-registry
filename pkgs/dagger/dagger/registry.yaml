packages:
  - type: github_release
    repo_owner: dagger
    repo_name: dagger
    description: A portable devkit for CI/CD pipelines
    format: tar.gz
    asset: "dagger_{{.Version}}_{{.OS}}_{{.Arch}}.{{.Format}}"
    overrides:
      - goos: windows
        format: zip
