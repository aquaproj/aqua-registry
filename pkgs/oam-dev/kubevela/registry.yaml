packages:
  - type: github_release
    repo_owner: oam-dev
    repo_name: kubevela
    asset: vela-{{.Version}}-{{.OS}}-amd64.tar.gz
    description: The Modern Application Deployment System Based on OAM
    files:
      - name: vela
        src: "{{.OS}}-amd64/vela"
