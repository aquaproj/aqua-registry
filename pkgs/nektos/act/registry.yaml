packages:
  - type: github_release
    repo_owner: nektos
    repo_name: act
    asset: "act_{{title .OS}}_{{.Arch}}.tar.gz"
    description: Run your GitHub Actions locally
    replacements:
      amd64: x86_64
