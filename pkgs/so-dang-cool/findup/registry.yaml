packages:
  - type: github_release
    repo_owner: so-dang-cool
    repo_name: findup
    description: Find parent directories
    asset: "findup-{{.Arch}}-{{.OS}}-gnu.{{.Format}}"
    format: tgz
    replacements:
      amd64: x86_64
      arm64: aarch64
    supported_envs:
      - linux
