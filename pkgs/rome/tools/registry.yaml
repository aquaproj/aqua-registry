packages:
  - type: github_release
    repo_owner: rome
    repo_name: tools
    description: Unified developer tools for JavaScript, TypeScript, and the web
    version_prefix: cli/
    asset: rome-{{.OS}}-{{.Arch}}
    format: raw
    files:
      - name: rome
    replacements:
      amd64: x64
      windows: win32
