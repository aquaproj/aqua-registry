packages:
  - type: github_release
    repo_owner: hatoo
    repo_name: oha
    description: Ohayou(おはよう), HTTP load generator, inspired by rakyll/hey with tui animation
    asset: oha-{{.OS}}-{{.Arch}}
    format: raw
    replacements:
      darwin: macos
    supported_envs:
      - darwin
      - linux
      - amd64
