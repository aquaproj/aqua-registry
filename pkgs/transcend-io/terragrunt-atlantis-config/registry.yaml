packages:
  - type: github_release
    repo_owner: transcend-io
    repo_name: terragrunt-atlantis-config
    description: Generate Atlantis config for Terragrunt projects
    version_constraint: "false"
    version_overrides:
      - version_constraint: semver("<= 0.10.1")
        no_asset: true
      - version_constraint: Version == "v0.11.0"
        asset: terragrunt-atlantis-config_{{trimV .Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        format: zip
        rosetta2: true
        windows_arm_emulation: true
        files:
          - name: terragrunt-atlantis-config
            src: "{{.AssetWithoutExt}}/{{.AssetWithoutExt}}"
        overrides:
          - goos: linux
            format: tar.gz
          - goos: windows
            files:
              - name: terragrunt-atlantis-config
                src: "{{.AssetWithoutExt}}"
        supported_envs:
          - darwin
          - windows
          - amd64
      - version_constraint: Version == "v0.12.0"
        no_asset: true
      - version_constraint: Version == "v0.13.0"
        asset: terragrunt-atlantis-config_{{trimV .Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        format: zip
        rosetta2: true
        windows_arm_emulation: true
        files:
          - name: terragrunt-atlantis-config
            src: "{{.AssetWithoutExt}}/{{.AssetWithoutExt}}"
        overrides:
          - goos: linux
            format: tar.gz
          - goos: windows
            files:
              - name: terragrunt-atlantis-config
                src: "{{.AssetWithoutExt}}"
        supported_envs:
          - darwin
          - windows
          - amd64
      - version_constraint: Version == "v1.0.0"
        no_asset: true
      - version_constraint: semver("<= 1.3.1")
        asset: terragrunt-atlantis-config_{{trimV .Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        format: zip
        rosetta2: true
        windows_arm_emulation: true
        files:
          - name: terragrunt-atlantis-config
            src: "{{.AssetWithoutExt}}/{{.AssetWithoutExt}}"
        overrides:
          - goos: linux
            format: tar.gz
          - goos: windows
            files:
              - name: terragrunt-atlantis-config
                src: "{{.AssetWithoutExt}}"
        supported_envs:
          - darwin
          - windows
          - amd64
      - version_constraint: Version == "v1.4.0"
        no_asset: true
      - version_constraint: semver("<= 1.7.0")
        asset: terragrunt-atlantis-config_{{trimV .Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        format: zip
        rosetta2: true
        windows_arm_emulation: true
        files:
          - name: terragrunt-atlantis-config
            src: "{{.AssetWithoutExt}}/{{.AssetWithoutExt}}"
        overrides:
          - goos: linux
            format: tar.gz
          - goos: windows
            files:
              - name: terragrunt-atlantis-config
                src: "{{.AssetWithoutExt}}"
        supported_envs:
          - darwin
          - windows
          - amd64
      - version_constraint: Version == "v1.7.1"
        no_asset: true
      - version_constraint: Version == "v1.7.2"
        asset: terragrunt-atlantis-config_{{trimV .Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        format: zip
        rosetta2: true
        windows_arm_emulation: true
        files:
          - name: terragrunt-atlantis-config
            src: "{{.AssetWithoutExt}}/{{.AssetWithoutExt}}"
        checksum:
          type: github_release
          asset: SHA256SUMS
          algorithm: sha256
        overrides:
          - goos: linux
            format: tar.gz
          - goos: windows
            files:
              - name: terragrunt-atlantis-config
                src: "{{.AssetWithoutExt}}"
        supported_envs:
          - darwin
          - windows
          - amd64
      - version_constraint: Version == "v1.8.1"
        asset: terragrunt-atlantis-config_{{trimV .Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        format: zip
        rosetta2: true
        windows_arm_emulation: true
        files:
          - name: terragrunt-atlantis-config
            src: "{{.AssetWithoutExt}}/{{.AssetWithoutExt}}"
        checksum:
          type: github_release
          asset: SHA256SUMS-2f921ee5fe12796141f6a8e056b44c29
          algorithm: sha256
        overrides:
          - goos: linux
            format: tar.gz
          - goos: windows
            files:
              - name: terragrunt-atlantis-config
                src: "{{.AssetWithoutExt}}"
        supported_envs:
          - darwin
          - windows
          - amd64
      - version_constraint: Version == "v1.9.0"
        asset: terragrunt-atlantis-config_{{trimV .Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        format: zip
        rosetta2: true
        windows_arm_emulation: true
        files:
          - name: terragrunt-atlantis-config
            src: "{{.AssetWithoutExt}}/{{.AssetWithoutExt}}"
        checksum:
          type: github_release
          asset: SHA256SUMS-4c4b09967baa659cdb8bfe82492bed58
          algorithm: sha256
        overrides:
          - goos: linux
            format: tar.gz
          - goos: windows
            files:
              - name: terragrunt-atlantis-config
                src: "{{.AssetWithoutExt}}"
        supported_envs:
          - darwin
          - windows
          - amd64
      - version_constraint: Version == "v1.9.1"
        asset: terragrunt-atlantis-config_{{trimV .Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        format: zip
        rosetta2: true
        windows_arm_emulation: true
        files:
          - name: terragrunt-atlantis-config
            src: "{{.AssetWithoutExt}}/{{.AssetWithoutExt}}"
        checksum:
          type: github_release
          asset: SHA512SUMS
          algorithm: sha512
        overrides:
          - goos: linux
            format: tar.gz
          - goos: windows
            files:
              - name: terragrunt-atlantis-config
                src: "{{.AssetWithoutExt}}"
        supported_envs:
          - darwin
          - windows
          - amd64
      - version_constraint: semver("<= 1.11.0")
        asset: terragrunt-atlantis-config_{{trimV .Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        format: zip
        rosetta2: true
        windows_arm_emulation: true
        files:
          - name: terragrunt-atlantis-config
            src: "{{.AssetWithoutExt}}/{{.AssetWithoutExt}}"
        checksum:
          type: github_release
          asset: SHA256SUMS
          algorithm: sha256
        overrides:
          - goos: linux
            format: tar.gz
          - goos: windows
            files:
              - name: terragrunt-atlantis-config
                src: "{{.AssetWithoutExt}}"
        supported_envs:
          - darwin
          - windows
          - amd64
      - version_constraint: Version == "v1.12.1"
        asset: terragrunt-atlantis-config_{{trimV .Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        format: zip
        rosetta2: true
        windows_arm_emulation: true
        files:
          - name: terragrunt-atlantis-config
            src: "{{.AssetWithoutExt}}/{{.AssetWithoutExt}}"
        checksum:
          type: github_release
          asset: SHA512SUMS
          algorithm: sha512
        overrides:
          - goos: linux
            format: tar.gz
          - goos: windows
            files:
              - name: terragrunt-atlantis-config
                src: "{{.AssetWithoutExt}}"
        supported_envs:
          - darwin
          - windows
          - amd64
      - version_constraint: Version == "v1.12.2"
        asset: terragrunt-atlantis-config_{{trimV .Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        format: zip
        rosetta2: true
        windows_arm_emulation: true
        files:
          - name: terragrunt-atlantis-config
            src: "{{.AssetWithoutExt}}/{{.AssetWithoutExt}}"
        checksum:
          type: github_release
          asset: SHA256SUMS
          algorithm: sha256
        overrides:
          - goos: linux
            format: tar.gz
          - goos: windows
            files:
              - name: terragrunt-atlantis-config
                src: "{{.AssetWithoutExt}}"
        supported_envs:
          - darwin
          - windows
          - amd64
      - version_constraint: Version == "v1.12.3"
        asset: terragrunt-atlantis-config_{{trimV .Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        format: zip
        rosetta2: true
        windows_arm_emulation: true
        files:
          - name: terragrunt-atlantis-config
            src: "{{.AssetWithoutExt}}/{{.AssetWithoutExt}}"
        checksum:
          type: github_release
          asset: SHA512SUMS-dff0de76bf7559983d4452185e20a128
          algorithm: sha512
        overrides:
          - goos: linux
            format: tar.gz
          - goos: windows
            files:
              - name: terragrunt-atlantis-config
                src: "{{.AssetWithoutExt}}"
        supported_envs:
          - darwin
          - windows
          - amd64
      - version_constraint: semver("<= 1.14.2")
        asset: terragrunt-atlantis-config_{{trimV .Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        format: zip
        rosetta2: true
        windows_arm_emulation: true
        files:
          - name: terragrunt-atlantis-config
            src: "{{.AssetWithoutExt}}/{{.AssetWithoutExt}}"
        checksum:
          type: github_release
          asset: SHA256SUMS
          algorithm: sha256
        overrides:
          - goos: linux
            format: tar.gz
          - goos: windows
            files:
              - name: terragrunt-atlantis-config
                src: "{{.AssetWithoutExt}}"
        supported_envs:
          - darwin
          - windows
          - amd64
      - version_constraint: Version == "v1.15.0"
        asset: terragrunt-atlantis-config_{{trimV .Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        format: zip
        rosetta2: true
        windows_arm_emulation: true
        checksum:
          type: github_release
          asset: SHA512SUMS-194e89d5f8dfc7d740212a9b2bacdc5b
          algorithm: sha512
        files:
          - name: terragrunt-atlantis-config
            src: "{{.AssetWithoutExt}}/{{.AssetWithoutExt}}"
        overrides:
          - goos: linux
            format: tar.gz
          - goos: windows
            files:
              - name: terragrunt-atlantis-config
                src: "{{.AssetWithoutExt}}"
        supported_envs:
          - darwin
          - windows
          - amd64
      - version_constraint: Version == "v1.16.0"
        asset: terragrunt-atlantis-config_{{trimV .Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        format: zip
        rosetta2: true
        windows_arm_emulation: true
        files:
          - name: terragrunt-atlantis-config
            src: "{{.AssetWithoutExt}}/{{.AssetWithoutExt}}"
        checksum:
          type: github_release
          asset: SHA512SUMS
          algorithm: sha512
        overrides:
          - goos: linux
            format: tar.gz
          - goos: windows
            files:
              - name: terragrunt-atlantis-config
                src: "{{.AssetWithoutExt}}"
        supported_envs:
          - darwin
          - windows
          - amd64
      - version_constraint: semver("<= 1.17.2")
        no_asset: true
      - version_constraint: semver("<= 1.17.4")
        asset: terragrunt-atlantis-config_{{trimV .Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        format: zip
        rosetta2: true
        windows_arm_emulation: true
        files:
          - name: terragrunt-atlantis-config
            src: "{{.AssetWithoutExt}}/{{.AssetWithoutExt}}"
        checksum:
          type: github_release
          asset: SHA256SUMS
          algorithm: sha256
        overrides:
          - goos: linux
            format: tar.gz
          - goos: windows
            files:
              - name: terragrunt-atlantis-config
                src: "{{.AssetWithoutExt}}"
        supported_envs:
          - darwin
          - windows
          - amd64
      - version_constraint: "true"
        asset: terragrunt-atlantis-config_{{trimV .Version}}_{{.OS}}_{{.Arch}}
        format: raw
        checksum:
          type: github_release
          asset: SHA512SUMS
          algorithm: sha512
