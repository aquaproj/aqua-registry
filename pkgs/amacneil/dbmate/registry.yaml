packages:
  - type: github_release
    repo_owner: amacneil
    repo_name: dbmate
    description: A lightweight, framework-agnostic database migration tool
    format: raw
    asset: "dbmate-{{.OS}}-{{.Arch}}"
    replacements:
      darwin: macos
