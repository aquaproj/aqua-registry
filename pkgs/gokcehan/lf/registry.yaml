packages:
  - type: github_release
    repo_owner: gokcehan
    repo_name: lf
    rosetta2: true
    description: Terminal file manager
    asset: "lf-{{.OS}}-{{.Arch}}.{{.Format}}"
    format: tar.gz
    format_overrides:
      - goos: windows
        format: zip
