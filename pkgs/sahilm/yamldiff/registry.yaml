packages:
  - type: github_release
    repo_owner: sahilm
    repo_name: yamldiff
    format: raw
    asset: "yamldiff-v{{.Version}}-{{.OS}}-amd64"
    description: A CLI tool to diff two YAML files
    files:
      - name: yamldiff
        src: "yamldiff-v{{.Version}}-{{.OS}}-amd64"
