packages:
  - type: github_release
    repo_owner: rancher
    repo_name: k3d
    asset: k3d-{{.OS}}-{{.Arch}}
    format: raw
    description: Little helper to run Rancher Lab's k3s in Docker
    supported_envs:
      - linux
      - darwin
      - amd64
