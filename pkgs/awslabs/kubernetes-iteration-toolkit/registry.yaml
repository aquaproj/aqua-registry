packages:
  - type: github_release
    repo_owner: awslabs
    repo_name: kubernetes-iteration-toolkit
    asset: kitctl_{{.Version}}_{{.OS}}_{{.Arch}}.zip
    supported_envs:
      - darwin
    rosetta2: true
    files:
      - name: kitctl
