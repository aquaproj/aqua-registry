packages:
  - type: github_release
    repo_owner: fastly
    repo_name: cli
    asset: fastly_{{.Version}}_{{.OS}}-{{.Arch}}.{{.Format}}
    format: tar.gz
    description: A CLI for interacting with the Fastly platform
    overrides:
      - goos: windows
        format: zip
    files:
      - name: fastly
