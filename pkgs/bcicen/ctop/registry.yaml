packages:
  - type: github_release
    repo_owner: bcicen
    repo_name: ctop
    rosetta2: true
    asset: "ctop-{{trimV .Version}}-{{.OS}}-{{.Arch}}"
    description: Top-like interface for container metrics
    format: raw
