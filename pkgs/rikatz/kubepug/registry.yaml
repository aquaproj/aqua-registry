packages:
  - type: github_release
    repo_owner: rikatz
    repo_name: kubepug
    asset: "kubepug_{{.OS}}_{{.Arch}}.{{.Format}}"
    description: Kubernetes PreUpGrade (Checker)
    format: tar.gz
    format_overrides:
      - goos: windows
        format: zip
