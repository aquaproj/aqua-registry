packages:
- name: grpc-ecosystem/grpc-gateway/protoc-gen-grpc-gateway
  type: github_release
  repo_owner: grpc-ecosystem
  repo_name: grpc-gateway
  description: Generate golang code to reverse proxy Protocol Buffer definitions to RESTful HTTP/1
  asset: 'protoc-gen-grpc-gateway-{{.Version}}-{{.OS}}-{{.Arch}}'
  supported_if: not (GOARCH == "arm64")
  rosetta2: true
  format: raw
  replacements:
    amd64: x86_64
  files:
  - name: protoc-gen-grpc-gateway
    src: 'protoc-gen-grpc-gateway-{{.Version}}-{{.OS}}-{{.Arch}}'
- name: grpc-ecosystem/grpc-gateway/protoc-gen-openapiv2
  type: github_release
  repo_owner: grpc-ecosystem
  repo_name: grpc-gateway
  description: Generate Swagger / OpenAPIv2 JSON file from Protocol Buffer definition
  asset: 'protoc-gen-openapiv2-{{.Version}}-{{.OS}}-{{.Arch}}'
  supported_if: not (GOARCH == "arm64")
  rosetta2: true
  format: raw
  replacements:
    amd64: x86_64
  files:
  - name: protoc-gen-openapiv2
    src: 'protoc-gen-openapiv2-{{.Version}}-{{.OS}}-{{.Arch}}'
  version_constraint: 'semver(">= 2.0.0")'
  version_overrides:
  - version_constraint: 'semver("< 2.0.0")'
    asset: 'protoc-gen-swagger-{{.Version}}-{{.OS}}-{{.Arch}}'
    files:
    - name: protoc-gen-swagger
      src: 'protoc-gen-swagger-{{.Version}}-{{.OS}}-{{.Arch}}'
