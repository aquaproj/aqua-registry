packages:
  - type: github_release
    repo_owner: snyk
    repo_name: driftctl
    asset: "driftctl_{{.OS}}_{{.Arch}}"
    description: Detect, track and alert on infrastructure drift
    format: raw
    replacements:
      armv6: arm
