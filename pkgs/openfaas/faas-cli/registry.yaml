packages:
  - type: github_release
    repo_owner: openfaas
    repo_name: faas-cli
    format: raw
    description: Official CLI for OpenFaaS
    asset: faas-cli-{{.Arch}}
    overrides:
      - goos: darwin
        asset: faas-cli-darwin
      - goarch: amd64
        asset: faas-cli
