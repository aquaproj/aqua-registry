packages:
- type: github_release
  repo_owner: sclevine
  repo_name: yj
  description: Convert between YAML, TOML, JSON, and HCL
  replacements:
    darwin: macos
  asset: 'yj-{{.OS}}'
  overrides:
  - goos: linux
    goarch: arm64
    asset: yj-linux-arm64
