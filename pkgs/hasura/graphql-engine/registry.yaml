packages:
  - type: github_release
    repo_owner: hasura
    repo_name: graphql-engine
    asset: "cli-hasura-{{.OS}}-{{.Arch}}"
    format: raw
    description: Hasura GraphQL Engine CLI
    files:
      - name: hasura
