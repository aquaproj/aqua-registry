packages:
  - type: http
    name: trunk-io/cli
    url: https://trunk.io/releases/{{.Version}}/trunk-{{.Version}}-{{.OS}}-{{.Arch}}.{{.Format}}
    format: tar.gz
    files:
      - name: trunk
    supported_envs:
      - darwin
      - amd64
    replacements:
      amd64: x86_64
