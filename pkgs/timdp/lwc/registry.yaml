packages:
  - type: github_release
    repo_owner: timdp
    repo_name: lwc
    description: A live-updating version of the UNIX wc command
    rosetta2: true
    asset: "lwc-{{trimV .Version}}-{{.OS}}-{{.Arch}}.tar.gz"
    supported_if: not (GOOS == "darwin" and GOARCH == "arm64")
