packages:
  - type: github_release
    repo_owner: junegunn
    repo_name: fzf
    asset: "fzf-{{.Version}}-{{.OS}}_{{.Arch}}.{{.Format}}"
    description: A command-line fuzzy finder
    format: tar.gz
    format_overrides:
      - goos: darwin
        format: zip
