packages:
  - type: github_release
    repo_owner: junegunn
    repo_name: fzf
    asset: "fzf-{{.Version}}-{{.OS}}_{{.Arch}}.{{.Format}}"
    description: A command-line fuzzy finder
    format: zip
    overrides:
      - goos: linux
        format: tar.gz
