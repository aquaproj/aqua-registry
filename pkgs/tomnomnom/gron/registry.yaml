packages:
  - type: github_release
    repo_owner: tomnomnom
    repo_name: gron
    asset: gron-{{.OS}}-{{.Arch}}-{{trimV .Version}}.{{.Format}}
    format: tgz
    description: Make JSON greppable
    overrides:
      - goos: windows
        format: zip
