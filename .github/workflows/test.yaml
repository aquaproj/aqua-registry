---
name: test
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  default:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: cache ~/.aqua
        uses: actions/cache@v2
        with:
          path: |
            ~/.aqua/pkgs
            ~/.aqua/registries
          key: v2-${{ runner.os }}-${{ hashFiles('registry.yaml') }}-${{ hashFiles('aqua-all.yaml') }}
          restore-keys: |
            v2-${{ runner.os }}-${{ hashFiles('registry.yaml') }}-
            v2-${{ runner.os }}-

      - uses: suzuki-shunsuke/aqua-installer@main
        with:
          version: v0.7.13-0 # renovate: depName=suzuki-shunsuke/aqua
      - run: aqua -c aqua-all.yaml i --test
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
